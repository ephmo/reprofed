metadata:
  name: kde
  description: Fedora KDE Plasma Workspaces profile
  version: "1.0.0"
  maintainer: ephmo
  release_date: "2025-12-30"
  last_modified: "2025-12-30"

requires:
  distro: fedora
  distro_versions:
    - "42"
    - "43"
  arch: x86_64

repos:
  rpmfusion-free: true
  rpmfusion-nonfree: true
  vscode: true

packages:
  install:
    all_versions:
      - "@admin-tools"
      - "@base-graphical"
      - "@container-management"
      - "@desktop-accessibility"
      - "@dial-up"
      - "@firefox"
      - "@fonts"
      - "@guest-desktop-agents"
      - "@hardware-support"
      - "@input-methods"
      - "@kde-desktop"
      - "@libreoffice"
      - "@multimedia"
      - "@networkmanager-submodules"
      - "@printing"
      - "@standard"
      - flatpak

services:
  set-default:
    - graphical.target

  enable:
    - sddm.service
